# Lab 02: Discovery and Model Quality

Compare frequency-filtered directly-follows models using held-out fitness,
precision, simplicity, and temporal stability. Pareto analysis replaces a
single arbitrary discovery threshold.

Run:

```bash
python3 labs/02-discovery-model-quality/discovery_quality_lab.py
```
